{
  "name": "evoia-mrp-warehouse",
  "version": "0.1.0",
  "private": true,
  "description": "Integrated MRP + Warehouse platform, multi-tenant by Business Unit. Vercel web app with a shared Postgres backend and two front-end modules.",
  "scripts": {
    "test": "SKIP_TSC=1 bash mrp/tools/run-tests.sh && node api/test/tenancy.test.js && node api/test/catalog.test.js && node api/test/receipts.test.js && node warehouse/test/placement.test.js",
    "test:full": "bash mrp/tools/run-tests.sh && node api/test/tenancy.test.js && node api/test/catalog.test.js && node api/test/receipts.test.js && node warehouse/test/placement.test.js",
    "build:mrp": "esbuild mrp/entry.jsx --bundle --format=iife --outfile=mrp/app.bundle.js --define:process.env.NODE_ENV=\\\"production\\\" --minify"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "lucide-react": "^1.28.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "typescript": "^5.5.4"
  }
}
